{% extends 'layout.njk' %}

{% block content %}

  <div class="card table-container">

    <a href="/teachers/create">Novo</a>
    <table width="100%">
      <thead>
        <tr>
          <th>Fotinho</th>
          <th>Instructor</th>
          <th>Atendimento</th>
          <th>Visualizar</th>
        </tr>
      </thead>

      <tbody>

        {% for teacher in teachers %}
          <tr>
            <td>
              <span style="background:url({{teacher.avatar_url}});"></span>
            </td>
            <td>{{teacher.name}}</td>
            <td>{{teacher.services}}</td>
            <td>
              <a href="/teachers/{{teacher.id}}">
                Visualize o Instrutor</a>
            </td>
          </tr>
          {%endfor%}
        </tbody>
      </table>
    </div>

  {% endblock content %}